# UTF-8 中文输入输出
# 文件作用：本文件为任务单，只能看，不要修改它，修改了视为未完成。
## 规则
- 用中文输入输出
- 这项目现在要做windows适配
- 如果写测试代码写在 `test` 目录，用完删掉
- 写代码要让代码在使用的时候不要猜测数据。错了就报错
- 所有工单或者do.md 里面有些什么第一步先做什么还有留着以后在做什么的，统统一竿子插到底，从根本上解决问题，不做过度阶段的修改，浪费时间
- 尽量不用现有数据做测试，如果用了测试完要恢复测试之前的样子
- **如果有什么参数硬编码的做成参数变量放到 config里面并且在后台设置面板里面可以加以配置，配置参数要备注详细**
- ui设计能用一行写完不用两行，lable文字和文本框写在同一行
- 上传服务器的时候用oss加速
- 有弹窗需求的复用弹窗类js，以及所有的前端ui都复用现有的js

## 工作清单

- 服务器
    - 
- 客户端
    - 不要跟网页混合起来，客户端就是客户端，登录的时候只是去网页数据库鉴权一下，没有什么过期不过期的，记录在内存里就行。除非退出登录
    - 研究一下拼多多的店铺怎么扫描，先别动手，给个方案


## 收尾工作
1. 输出一下执行结果，是否达到预期，最后务必一条一条列出来，达到预期的用✅标记
2. 如果有增加类或函数或功能，在`tree.md`文件加上树状结构,记录所有文件所有类所有函数的简单描述
3. 如果需要重启服务才能改变的就重启服务
4. 项目promt.md：如果车间有改动代码改变了逻辑，把相应的变动merge进`promt.md`里面，注意，**不是把每次的变更记录黏在后面，而是要把变更后的最终版本覆盖掉原来的内容，保证**`promt.md`**里永远是最新的逻辑版本**。